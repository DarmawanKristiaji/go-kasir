# Environment Configuration
# Copy this file to .env and fill with your actual values

# Server Port (Railway sets this automatically in production)
PORT=8080

# Database Connection String
# For Supabase Transaction Pooler (Recommended for Railway)
DB_CONN=host=your-pooler-host.pooler.supabase.com port=6543 user=postgres.your-project password=your-password dbname=postgres sslmode=require options=-c search_path=public

# Alternative: Direct Supabase Connection (Local Development)
# DB_CONN=host=db.your-project.supabase.co port=5432 user=postgres password=your-password dbname=postgres sslmode=require

# Alternative: Session Pooler
# DB_CONN=host=your-pooler-host.pooler.supabase.com port=5432 user=postgres.your-project password=your-password dbname=postgres sslmode=require

# Notes:
# - Never commit .env file with actual credentials
# - Use transaction pooler (port 6543) for Railway deployment
# - The 'options=-c search_path=public' is required for transaction pooler
# - Railway automatically sets PORT variable
